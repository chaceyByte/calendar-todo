# 任务活动记录功能

## 功能概述

任务活动记录功能用于跟踪和记录任务的生命周期活动，包括创建、开始、暂停、恢复、完成等状态变化，以及手动添加的工作时间记录。这些记录可用于生成日报和周报，帮助用户分析工作时间和效率。

## 核心设计

### 活动记录实体

活动记录(ActivityRecord)包含以下字段：
- `id`: 记录ID
- `taskId`: 关联的任务ID
- `startTime`: 活动开始时间
- `endTime`: 活动结束时间
- `activityType`: 活动类型（创建、开始、暂停、恢复、完成、工作、会议、学习等）
- `description`: 活动描述
- `durationMinutes`: 活动持续时间（分钟）
- `createdAt`: 记录创建时间

### 活动类型

系统支持以下活动类型：
- `CREATED`: 任务创建
- `STARTED`: 任务开始
- `PAUSED`: 任务暂停
- `RESUMED`: 任务恢复
- `COMPLETED`: 任务完成
- `WORK`: 工作时间
- `MEETING`: 会议时间
- `STUDY`: 学习时间
- `OTHER`: 其他活动

## 功能特性

### 自动记录

系统会在以下情况自动记录活动：
1. 任务创建时记录"创建"活动
2. 任务状态变更时记录相应的活动（开始、暂停、恢复、完成）
3. 任务拖拽到不同状态列时自动记录状态变更

### 手动记录

用户可以手动添加活动记录，用于补充系统无法自动捕获的工作时间：
1. 在任务卡片右键菜单中选择"添加活动记录"
2. 填写活动类型、开始时间、结束时间和描述
3. 系统自动计算活动持续时间

### 活动查看

1. **任务卡片指示器**：每个任务卡片底部显示当前活动状态和持续时间
2. **活动记录详情**：点击活动指示器或右键菜单中的"查看活动记录"查看完整活动历史
3. **活动统计**：显示总活动时间和实际工作天数

### 报告生成

系统支持生成日报和周报：

#### 日报
- 显示指定日期的活动统计
- 包含总活动时间、完成任务数、活动任务数
- 列出当天所有任务的活动详情

#### 周报
- 显示指定周的活动统计
- 包含每日活动时间柱状图
- 列出本周所有任务的活动详情

## 使用方法

### 1. 查看任务活动状态

在任务看板页面，每个任务卡片底部会显示当前活动状态：
- 蓝色表示进行中的活动
- 橙色表示暂停状态
- 绿色表示已完成
- 灰色表示计划中

### 2. 查看详细活动记录

1. 点击任务卡片底部的活动状态指示器
2. 在右侧抽屉中查看该任务的完整活动历史
3. 查看活动统计信息

### 3. 手动添加活动记录

1. 在任务卡片上右键，选择"添加活动记录"
2. 填写活动信息：
   - 选择活动类型（工作、会议、学习等）
   - 设置开始和结束时间
   - 添加描述（可选）
3. 点击"保存"添加记录

### 4. 查看活动报告

1. 点击顶部导航栏的"报告"
2. 选择"日报"或"周报"标签
3. 选择日期或周范围
4. 查看活动统计和详情

### 5. 暂停和恢复任务

1. 在任务卡片右键菜单中选择"暂停"或"恢复"
2. 系统自动记录暂停和恢复活动
3. 暂停期间不会计入活动时间

## 技术实现

### 后端实现

1. **实体类**：`ActivityRecord.java` - 使用MyBatis-Plus注解
2. **枚举**：`ActivityType.java`
3. **Mapper**：`ActivityRecordMapper.java` 继承MyBatis-Plus的BaseMapper，和对应的XML映射文件
4. **服务类**：`ActivityService.java` 继承MyBatis-Plus的ServiceImpl
5. **控制器**：`ActivityController.java`
6. **DTO类**：`StartActivityRequest.java`、`ManualActivityRequest.java`、`DailyReport.java`、`WeeklyReport.java`

### 前端实现

1. **Store**：`activity.ts` - 管理活动记录状态
2. **视图组件**：
   - `Tasks.vue` - 任务卡片上的活动指示器和活动记录抽屉
   - `Reports.vue` - 日报和周报页面
3. **路由配置**：添加了Reports页面路由

### 数据库

活动记录表通过`V2__Add_activity_records_table.sql`迁移脚本创建，包含必要的索引和外键约束。

## 注意事项

1. 活动记录与任务状态变更紧密关联，修改任务状态时会自动记录相应活动
2. 手动添加的活动记录不会影响任务状态
3. 暂停任务会结束当前活动，恢复任务会开始新的活动
4. 活动时间计算基于开始和结束时间，精确到分钟
5. 报告数据基于活动记录生成，确保活动记录的准确性对报告质量至关重要

## 未来扩展

1. 添加活动记录的编辑和删除功能
2. 支持更详细的活动分类和标签
3. 添加活动时间的目标设定和对比分析
4. 支持导出报告为PDF或Excel格式
5. 添加活动时间提醒和统计图表